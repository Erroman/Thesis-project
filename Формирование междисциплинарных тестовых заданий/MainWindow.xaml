<Window x:Class="Формирование_междисциплинарных_тестовых_заданий.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Формирование_междисциплинарных_тестовых_заданий"
        mc:Ignorable="d"
        Title="Формирование междисциплинарных тестовых заданий" Height="450" Width="800" Loaded="Window_Loaded" >
    <Window.Resources>
        <CollectionViewSource x:Key="subjectViewSource"/>
        <CollectionViewSource x:Key="subjectQuestionsViewSource" 
                              Source="{Binding Questions, Source={StaticResource subjectViewSource}}"/>
    </Window.Resources>
    <Grid>
        <DataGrid x:Name="subjectDataGrid" AutoGenerateColumns="False" 
                  EnableRowVirtualization="True" 
                  ItemsSource="{Binding Source={StaticResource subjectViewSource}}" 
                  Margin="13,13,43,229" RowDetailsVisibilityMode="VisibleWhenSelected">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding SubjectId}"
                                    Header="Предмет Id" Width="SizeToHeader"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Binding="{Binding SubjectName}" Header="Предмет" 
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid x:Name="questionsDataGrid" AutoGenerateColumns="False" 
                  EnableRowVirtualization="True" 
                  ItemsSource="{Binding Source={StaticResource subjectQuestionsViewSource}}" 
                  Margin="13,205,43,108" RowDetailsVisibilityMode="VisibleWhenSelected" 
                  RenderTransformOrigin="0.488,0.251">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding QuestionId}" 
                                    Header="Вопрос Id" Width="SizeToHeader"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Binding="{Binding SubjectId}" Header="Предмет Id" 
                                    Width="SizeToHeader" IsReadOnly="True"/>
                <DataGridTextColumn Binding="{Binding QuestionText}" Header="Вопрос" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
